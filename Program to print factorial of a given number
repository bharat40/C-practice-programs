#include<stdio.h>  // Factorial 
int main()
{
    int fac = 1, num, i;
    printf("Enter a number = ");
    scanf("%d",&num);
    for(i=1;i<=num;i++)
    {
        fac=fac*i;
    }
    printf("Factorial of %d = %d", num, fac);
    return 0;
}
